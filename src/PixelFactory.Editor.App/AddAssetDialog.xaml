<Window x:Class="PixelFactory.Editor.App.AddAssetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Asset" Height="180" Width="320"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name:" VerticalAlignment="Center" Grid.Row="0"
                   Style="{StaticResource PropertyLabel}" />
        <TextBox x:Name="AssetNameBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" />

        <TextBlock Text="Type:" VerticalAlignment="Center" Grid.Row="1"
                   Style="{StaticResource PropertyLabel}" />
        <ComboBox x:Name="AssetTypeBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,8,0" Click="Ok_Click" IsDefault="True" />
            <Button Content="Cancel" Width="75" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
