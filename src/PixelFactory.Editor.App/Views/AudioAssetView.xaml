<UserControl x:Class="PixelFactory.Editor.App.Views.AudioAssetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource PanelBackgroundBrush}">
        <DockPanel Margin="8">
            <TextBlock DockPanel.Dock="Top"
                       Text="{Binding AssetName}"
                       FontSize="14"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,8" />

            <!-- Transport controls (hidden until playback is implemented) -->
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                        HorizontalAlignment="Center" Margin="0,8,0,0"
                        Visibility="Collapsed">
                <Button Content="Play" Width="60" Margin="2" IsEnabled="False" />
                <Button Content="Pause" Width="60" Margin="2" IsEnabled="False" />
                <Button Content="Stop" Width="60" Margin="2" IsEnabled="False" />
            </StackPanel>

            <!-- Waveform area -->
            <Border Background="Black" CornerRadius="4">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Waveform"
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding Duration, StringFormat='{}{0:F1}s'}"
                               FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>
