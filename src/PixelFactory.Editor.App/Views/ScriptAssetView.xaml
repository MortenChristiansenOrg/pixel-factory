<UserControl x:Class="PixelFactory.Editor.App.Views.ScriptAssetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource PanelBackgroundBrush}">
        <DockPanel Margin="8">
            <TextBlock DockPanel.Dock="Top"
                       Text="{Binding AssetName}"
                       FontSize="14"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,4" />

            <!-- Code editor area -->
            <Border Background="Black" CornerRadius="4">
                <TextBox x:Name="CodeEditor"
                         Text="{Binding SourceCode, UpdateSourceTrigger=PropertyChanged}"
                         FontFamily="Consolas"
                         FontSize="12"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         Background="Transparent"
                         BorderThickness="0"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" />
            </Border>
        </DockPanel>
    </Grid>
</UserControl>
